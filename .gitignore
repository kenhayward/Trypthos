node_modules/
dist/
dist-electron/
release/
out/
coverage/
*.tsbuildinfo
.DS_Store
Thumbs.db
.env
.env.local

# Failure screenshots written by the browser test suite.
__screenshots__/
.vitest-attachments/
# Vitest 5 writes failure screenshots and other attachments here instead.
.vitest/

# Tray icons, generated by apps/desktop/scripts/make-tray-icon.mjs
apps/desktop/build/

# A personal list of what to build next. Deliberately not in the repo: it is one person's working
# notes rather than a plan the project has agreed, and `docs/` is otherwise all published material.
docs/Roadmap Features.md

# The spec-driven-development working area: the plan ledger, task briefs, review diffs and the
# reports implementers write back. Scratch for building the thing, not part of the thing.
.superpowers/
